export const gameWrapper = document.querySelector(
  '.game-wrapper'
) as HTMLDivElement

export const game = document.querySelector('.game') as HTMLDivElement

export const totalWrapper = document.querySelector('.total') as HTMLDivElement

export const totalScore = document.querySelector(
  '.total__score span'
) as HTMLSpanElement

export const infoScore = document.querySelector(
  '.info__item--score span'
) as HTMLSpanElement

export const infoTimer = document.querySelector(
  '.info__item--timer span'
) as HTMLSpanElement

export const buttonStart = document.querySelector(
  '.button-start'
) as HTMLButtonElement
